'use strict';

import React from 'react';

const HalTitle = ( props ) => {

    return (

        <div className='hal-component'>
            <h1>{ props.title }</h1>
        </div>
    );
};

export default HalTitle;